services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "${PORT:-8001}:8000"
    environment:
      - PORT=8000
    env_file: ./backend/.env
    volumes:
      - ./data:/app/data
      - ./backend/service_account.json:/app/service_account.json
