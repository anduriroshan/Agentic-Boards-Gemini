services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "${PORT:-8000}:8000"
    environment:
      - PORT=8000
    env_file: ./backend/.env
    volumes:
      - ./data:/app/data
